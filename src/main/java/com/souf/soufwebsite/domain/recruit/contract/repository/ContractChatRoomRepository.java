package com.souf.soufwebsite.domain.recruit.contract.repository;

import com.souf.soufwebsite.domain.recruit.contract.entity.ContractChatRoom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContractChatRoomRepository extends JpaRepository<ContractChatRoom, Long> {
}
